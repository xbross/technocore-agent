# Secrets et etat local — ne jamais versionner
identity/
state/
logs/
.env
*.pem
*.key

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Paquet officiel du concours (telecharge et verifie par sonnet_cli.py fetch-package)
contest/package/
# Config personnelle du concours (DID, compte X) : ne pas versionner, copier depuis sonnet.example.toml
sonnet.toml
